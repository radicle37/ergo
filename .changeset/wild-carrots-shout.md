---
"ergo-state": minor
---

Add `createErgoStoreSelectorSource` for producing branded `{ get, subscribe }` selector-source pairs from an existing store selector, and export `ErgoStoreSelectorMapMarker` (plus the `ErgoStoreSelectorMapOf` type utility) to support external inference of a store's selector map.
